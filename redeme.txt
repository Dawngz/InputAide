输入助手项目：
1.功能
输入任意搜索内容，返回与它最相近的三个搜索选项
2.模块分类与功能
网络连接模块：主线程负责内容的输入并将它封装到任务队列，socket、bind、listen、accept获取描述符

线程池模块：维护任务队列，从epoll中获取描述符，添加到任务队列，将任务队列传给子线程

epoll模块：监控所有描述符，新描述符就位添加到监控队列，旧描述符就位添加到任务队列，有描述符断开则删除

子线程任务处理模块：获取描述符，完成任务，返回处理结果

工作模块：创建词典和索引，分词算法，优先级队列，最小编辑距离算法

需要用到：网络库、日志、lua、序列化